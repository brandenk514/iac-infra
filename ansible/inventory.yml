all:
  children:
    homelab:
      hosts:
        nanosaurus.local.uaccloud.com:
          ansible_user: core-ci
          ansible_ssh_private_key_file: ~/.ssh/ubuntu-server-key
        ptero.local.uaccloud.com:
          ansible_user: core-ci
          ansible_ssh_private_key_file: ~/.ssh/ubuntu-server-key
      children:
        traefik:
          hosts:
            trex.local.uaccloud.com:
              ansible_user: core-ci
              ansible_ssh_private_key_file: ~/.ssh/ubuntu-server-key
        gpu:
          hosts:
            trex.local.uaccloud.com: {}
