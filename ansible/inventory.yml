all:
  children:
    homelab:
      hosts:
        trex.local.uaccloud.com:
          ansible_user: core-ci
          ansible_ssh_private_key_file: ~/.ssh/ubuntu-server-key
        ptero.local.uaccloud.com:
          ansible_user: core-ci
          ansible_ssh_private_key_file: ~/.ssh/ubuntu-server-key
    # Hosts that run the Traefik container and need its on-disk config
    # rendered by the traefik role.
    traefik:
      children:
        homelab: {}
